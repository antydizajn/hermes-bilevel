# Bilevel model

## Inner loop

Fixed candidate + task + contract → trajectory → metrics → result.

## Outer loop

Evidence + lineage + failures → proposals → validate → isolated eval → multi-objective select.

Ordinary retry/reflection is **not** bilevel unless the outer level modifies
the mechanism controlling the inner level.
