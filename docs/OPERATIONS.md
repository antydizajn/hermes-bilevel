# Operations

State root: `$HERMES_HOME/bilevel/` (override `HERMES_BILEVEL_ROOT` or config storage.root).

```bash
hermes-bilevel init
hermes-bilevel doctor
hermes-bilevel status
hermes-bilevel integrity
```
