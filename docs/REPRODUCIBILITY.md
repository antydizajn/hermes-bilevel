# Reproducibility

Record: dataset hashes, purity registry hash, evaluator contract hash,
plugin/hermes versions, seeds, evaluation mode, fallback flags.

Changed dataset/evaluator/purity registry => NOT_COMPARABLE.
